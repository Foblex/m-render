import { ENVIRONMENT as DOC_ENV, ROUTES as DOC_ROUTES } from '@f-documentation-page';
import { ENVIRONMENT as HOME_ENV, ROUTES as HOME_ROUTES } from '@f-home-page';

export const DOCUMENTATION = {
  ENVIRONMENT: DOC_ENV,
  ROUTES: DOC_ROUTES,
};

export const HOME_PAGE = {
  ENVIRONMENT: HOME_ENV,
  ROUTES: HOME_ROUTES,
};

export { lazyComponent } from '@f-markdown';
